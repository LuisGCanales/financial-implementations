# Operational Scripts

This directory will contain stable workflows that build and export the selected project baseline.

The baseline will be implemented in a later phase as simultaneous nodal calibration with `CUBIC_CONTINUOUS_ZERO`.

No external consumer should depend on scripts under `experiments/`.
